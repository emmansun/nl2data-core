# calculated-field-semantics Tasks

## 1. CalculatedField model (views layer)

- [x] 1.1 Add frozen `ExprNode` and `CalculatedField` Pydantic models in `src/nl2data_core/views/models.py`: closed operator whitelist (`field`, `const`, `add`, `sub`, `mul`, `div`), `int`-only constants (float and bool rejected — fingerprint-domain discipline; negative ints valid), complete output inference table (`add`/`sub`/`mul` infer `int` only when both operands are `int`, otherwise `float`; `div` always infers `float`) with declared-equals-inferred enforcement (`CF_001` variant naming both types), `zero_division_policy` (`Literal["null", "error"]`, default `"null"`), exact order-free `requires` list (set semantics), depth ≤ 16 and node count ≤ 64 bounds; JSON-wire safe (nested dict/list canonical payload, no `frozenset`)
- [x] 1.2 Add optional entity-level `calculated_fields` member to `SemanticEntityDescriptor` (bounded count ≤ 32): N6 omit-when-unset in `canonical_payload()`, set-means-non-empty validator, descriptor-global unique names, no collision with descriptor field ids; explicit non-composition rule (an expression referencing another calculated field is a `CF_002` rejection); bidirectional pii isolation per design D11 — validator rejecting any expression reference to a `pii: true` field (`CF_004`, definition time) and bundle-level validator rejecting `pii` declarations applied to fields already referenced by a declared calculated field (`CF_004`, bundle validation time); expression hash helper (`sha256` over canonical tree + policy + output type). Future field-masking policy models must join the same D11 check.
- [x] 1.3 Unit tests: whitelist rejection (`CF_001` naming operator/bound), bounds, float/bool constant rejection and negative-constant acceptance, inference-table coverage (int×int → int, mixed → float, `div` → float, declared ≠ inferred rejected), output-type CAST contract, numeric-leaf type check, unknown reference and `requires` mismatch including order-independence (`CF_002`), cross-entity/self-reference rejection, name uniqueness and field-id collision, pii-reference rejection in both timing orders (`CF_004`: pii-field reference at definition time; pii applied over a referenced field rejected at bundle validation; either arrival order rejected) and unaffected non-referenced pii fields, CF-composition rejection (`CF_002`), JSON-wire safety, N6 omit-when-unset fingerprint identity (descriptor → snapshot → bundle), set-changes-fingerprint, snapshot-breaking chain for any calculated-field content change (reuse the `TestN6OmitWhenUnset` / `TestSnapshotBreakingChain` pattern from `tests/unit/test_value_semantics.py`)

## 2. IR reference rules and validation

- [x] 2.1 Add structured `CF_001` / `CF_002` / `CF_003` / `CF_004` / `CF_005` error codes (bounded, evidence-safe detail sets) to the error surfaces used by descriptor validation, IR validation, and runtime division-by-zero under the `error` policy
- [x] 2.2 Extend IR/view validation: a selection `field_id` resolves to a declared field or declared calculated field of the entity (undeclared → `CF_003` fail-closed); referencing a calculated field adds `calculated-fields` to `required_capabilities`; placeholder extension schema for `named_query_placeholder` (capability `named-query-placeholders`, payload: bounded `query_ref` identifier + typed scalar parameters `name`/`scalar_type in str|int|float|bool`/`required`, JSON-safe, no physical names)
- [x] 2.3 Contract tests: declared-CF selection builds a valid IR carrying the capability; undeclared reference → `CF_003`; no-collision disambiguation; unsupported-capability rejection path unchanged; placeholder without capability rejected fail-closed; invalid placeholder payload rejected structurally; IRs without the extension byte-identical (fingerprint pinned); `ir_version` unchanged

## 3. Compile-time expansion (compiler + adapters)

- [x] 3.1 Implement deterministic expression expansion in the compiler: re-validate tree fail-closed (`CF_001`), resolve `field` leaves to physical columns via `PhysicalBinding`, expand to SQL expression (SQLite + Postgres dialects, with explicit CAST enforcing the declared output type — including true division for integer operands) and MongoDB aggregation-pipeline output; aggregation kind applies uniformly to the expanded expression (including `none`, the row-level case); `zero_division_policy` semantics (`null` → NULL/missing via guarded expansion, `error` → structured `CF_005` execution failure); nothing interpreted at runtime and the expression never enters the IR
- [x] 3.2 Record `calculated_field_hashes: tuple[CalculatedFieldHash, ...]` (frozen name+hash record model — never a packed wire string) on `CompilationEvidence`, sorted by field name, hash covering canonical tree + policy + output type, bounded; omit-when-unset in the evidence canonical payload so evidence without calculated fields stays byte-identical
- [x] 3.3 Anchor the expansion implementation identity (design D12): define the `compiler_identity` constant, carry it on compilation context and evidence, and extend the pre-execution guard with the symmetric comparison mirroring v4.1's planner-identity guard (drift rejected, one-sided rejected both ways, both-unset legacy unchanged, strictness clause behind an identity-versioning switch); rollout order pinned — identity lands on producer (context) and consumer (evidence) sides in the same release, with the one-sided rejection activating only once both sides populate it
- [x] 3.4 Contract tests: expansion produces adapter-native output with no runtime evaluation; both `zero_division_policy` values behave as declared; evidence carries frozen hash records sorted by name with no expression material; the same IR compiled with different selection orders produces identical evidence fingerprints; evidence without calculated fields is fingerprint-stable; expansion-identity guard tests (drift, one-sided both ways, both-unset legacy, versioning-strict activation — mirror of `test_planner_identity_guard.py`); unsupported-capability adapters reject referencing queries fail-closed

## 4. Dual-adapter conformance

- [x] 4.1 Conformance suite in `tests/conformance/`: the same `CalculatedField` definitions over the controlled fixtures produce exact-value-equivalent results on the SQL and MongoDB adapters, including both `zero_division_policy` values, equivalent structured failures, and an `int / int` division fixture (e.g. `7 / 2` → `3.5`) that catches SQLite integer-division truncation; MongoDB tests follow the existing skip-gated pattern
- [x] 4.2 Document numeric-fidelity boundaries in the conformance suite (SQLite dynamic typing vs BSON int/double) and the exact-value assertion strategy

## 5. Instruction-bundle prompt context

- [x] 5.1 Add calculated-field identity (name, label, description, output type) to the model instruction bundle as bounded prompt context, subject to existing bounds and safe-content validation
- [x] 5.2 Tests: prompt context is bounded and safe-content validated; the model can only reference by name — expression material emitted by the model is rejected by existing structural validation (N4)

## 6. Evaluation attribution and corpus annotations

- [x] 6.1 Add bounded `CF_HIT` / `CF_COMPILE_FAIL` / `CF_NOT_DECLARED` / `CF_NOT_REFERENCED` attribution to the evaluation layer: per-selection records on `CaseEvidence`, per-case aggregation, per-run `EvaluationReport.calculated_field_summary()`; annotation-vs-actual deviations recorded, never crashes; evidence-safe serialization (no expressions, physical names, or values)
- [x] 6.2 Annotate `demo/questions/questions.yml` with calculated-field expectations (field, expected attribution) following the v4.1 annotation pattern; extend the file header with the deviation and sync-obligation rules
- [x] 6.3 Evaluation tests: annotated hit reports `CF_HIT`; expansion failure reports `CF_COMPILE_FAIL` with failing case outcome; undeclared-but-annotated reports `CF_NOT_DECLARED`; declared-but-unreferenced reports `CF_NOT_REFERENCED`; granularity per selection/case/run; serialized attribution is evidence-safe

## 7. Documentation

- [x] 7.1 ADR-045 (numbered from the merged ADR registry — DDS-020 already occupies 036–044): operator-whitelist rejections with alternative paths (function calls, subqueries, string ops, `CASE WHEN`, aggregate composition) and the aggregate-composition non-goal; dual-language like the pii ADR; create a unified ADR registry index (single list covering both documentation lines, e.g. `docs/architecture/adr-registry.md`) so future slices cannot collide
- [x] 7.2 Semantic-layer docs (EN + zh-CN): calculated-field capability overview, N6 optional-member checklist extended to name `CalculatedField`, snapshot-breaking upgrade checklist applicability, first-adoption guidance (assess adapter `calculated-fields` capability support before declaring), error codes `CF_001–CF_005` in the error-codes reference (including the bidirectional `CF_004` pii isolation rationale — definition-time and bundle-validation-time enforcement, plus the future masking-policy extension point — and the `CF_005` runtime zero-division code)
- [x] 7.3 Roadmap notes: v4.2 gate (combined `CF_NOT_DECLARED + CF_NOT_REFERENCED < 10%` and DSL expression failure rate `< 5%`, read from `calculated_field_summary()`) and the v4.1 precondition gate (`VS_HIT ≥ 90%`)
- [x] 7.4 NamedQuery reservation note: the reserved placeholder schema, its fail-closed v4.2 behavior, that placeholders are structurally inexpressible inside calculated-field expression trees (v4.4 must not open this when extending the whitelist), that runtime parameter values will need the v4.1 bool/int-subclass discipline, and that v4.4 may revise the reservation in its own change

## 8. Quality gates

- [x] 8.1 Run full pytest, ruff, mypy, and `check_docs`; run `openspec validate calculated-field-semantics --type change`
- [x] 8.2 Verify no fingerprint regressions: existing descriptor/snapshot/bundle/evidence fingerprints byte-identical across the full test suite
